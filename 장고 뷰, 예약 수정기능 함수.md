# 장고 뷰, 예약수정 기능 함수 

~~~ django
 if restaurant.reservation_seat >= total:  

		result = self.form_valid(form)     #form정보를 db에 저장해서 리다이렉트하는 객체를 result에 저장 나중에 return하려고

		c = self.get_object()								#현재 만들어진 객체를 return하도록 하는 역할

		c.customer_seatscount +=  form.instance.customer_update_seats 

		c.save()

		return result
~~~

